# Math-calculator
A simple c++ console application that represents a calculator
!NOTE! The current version only works on windows. If you still want to run it on another OS, delete all the system("cls) and system("pause") from the code. They are there only to make it look better
